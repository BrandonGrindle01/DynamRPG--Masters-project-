using UnityEngine;
using System;

public enum QuestType { Kill, Steal, Collect, Explore, Defend, Deliver }
public enum QuestStatus { Inactive, Active, Completed, Failed }
public enum Personality { Balanced, Aggressive, Criminal, Explorer, Pacifist }